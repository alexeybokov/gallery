=semantic_form_for :nokogiri, builder: ActiveAdmin::FormBuilder, url: { action: :grab } do |f|
  =f.inputs do
    =f.input :url, label: false, placeholder: 'Paste your URL', input_html: { style: 'width: 50%;' }
    =f.action :submit, label: 'Get Images'
