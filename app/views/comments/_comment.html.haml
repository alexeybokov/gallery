%i
  -if @comments.exists?
    -"This image has #{pluralize(@comments.count, "Comment") + ":"}"
    -@comments.each do |comment|
      .commentlistitem
        %ul
          %li
            =comment.body
          =comment.user.email
  -else
    ="No comments yet"


- if user_signed_in?
  = render 'comments/form'
