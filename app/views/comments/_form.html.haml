= form_for([@image, @image.comments.build]) do |f|
  .field
    .control
      = f.text_field :body, input_html: { class: 'textarea' }, wrapper: false, label_html: { class: 'label' }
  = f.submit 'Leave a reply', class: "button is-primary"
