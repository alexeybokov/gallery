<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @image.name %>
</p>

<p>
  <strong>Image:</strong>
  <%#= image_tag (@image.picture.thumb.url, alt: 'Image')# if @image.picture? %>

</p>

<%= render 'comments/comment' %>

<%= link_to 'Edit', edit_image_path(@image) %>
<%= link_to  'Back', images_path %>
