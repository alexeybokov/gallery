%h2
  = @image.name
%br
= image_tag(@image.picture.large.url, alt: 'some_image')
%br
%p
  = render 'hearts/button'
  %i.fas.fa-heart
    = @image.hearts_count

%br
= render 'comments/comment'# comments: @comments#, comments: @image.comments
