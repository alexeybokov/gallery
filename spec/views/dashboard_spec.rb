# frozen_string_literal: true

require 'rails_helper'

describe 'dashboard/index.html.haml' do
  it 'renders image' do
    skip
    # assign(:images, [stub_model(Image), stub_model(Image)])
    # render
    # expect(view).to render_template(partial: 'dashboard/index.html.haml')
  end
end
